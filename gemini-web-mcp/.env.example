# Copy to .env (git-ignored) for local runs. Never commit real values.
# Generate the path secret with:
#   python -c "import secrets,base64;print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode().rstrip('='))"
MCP_PATH_SECRET=
GEMINI_1PSID=
GEMINI_1PSIDTS=
DATA_DIR=./data
PORT=8080
# Optional:
# PUBLIC_BASE_URL=http://localhost:8080
# MAX_IMAGES_PER_HOUR=30
# SESSION_TTL_HOURS=24
# GEMINI_MODEL=
